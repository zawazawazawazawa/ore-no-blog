<h3><%= @diary.title %></h3>
<p><%= @diary.body %></p>

<% if user_signed_in? %>
  <%= link_to "Edit", edit_diary_path %><br>
  <%= link_to "Delete", diary_path, method: :delete, data:{confirm:"Are you sure?"} %>
<% end %>
